# URL base da API (packages/api). Sem barra final.
# Se a API ainda não estiver no ar, a UI carrega normalmente e mostra
# estados vazios/erro desenhados — nada quebra.
VITE_API_URL=http://localhost:3333
